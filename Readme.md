```shell
pip install mlx==0.30.0 nanobind cmake opencv-python plyfile
pip install submodules/spz
make pip-install
```

mlx custom extension on different version may show `incomplete function` in nanobind